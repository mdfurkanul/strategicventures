{
  "name": "site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "pages:setup": "wrangler pages project create strategic-ventures --production-branch=main",
    "deploy": "astro build && wrangler pages deploy dist --project-name=strategic-ventures --branch=main",
    "deploy:preview": "astro build && wrangler pages deploy dist --project-name=strategic-ventures --branch=preview",
    "pages:dev": "astro build && wrangler pages dev dist"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@tailwindcss/vite": "^4.3.2",
    "astro": "^7.0.7",
    "tailwindcss": "^4.3.2"
  }
}
